package com.example.spacestationv2.Model;
/*
import androidx.lifecycle.LiveData;
import androidx.room.Dao;
import androidx.room.Delete;
import androidx.room.Insert;
import androidx.room.Query;
import androidx.room.Update;

import java.util.List;

@Dao
public interface TemperatureDao {

    @Insert
    void insertTemp(Temperature temperature);

    @Update
    void updateTemp(Temperature temperature);

    @Delete
    void deleteTemp(Temperature temperature);

    @Query("DELETE FROM temperature_table")
    void deleteAllTemperature();

    @Query("SELECT * FROM temperature_table")
    LiveData<List<Temperature>> getAllTemperature();
}

 */
